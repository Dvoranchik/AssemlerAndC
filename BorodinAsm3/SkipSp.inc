 ;
; Процедура, пропуска пробельных символов в начале строки
;
; Вход:
;		DS:ESI - строка
; Выход:
;		DS:ESI - строка
;
SkipSpace proc
@@Loop:
			lodsb 						; считываем символ в AL
			cmp		al, ' '				; Проверяем пробел ли это
			je		@@Loop			; если да - идем дальше
			ret
			
SkipSpace endp
